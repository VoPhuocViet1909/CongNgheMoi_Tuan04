<div class="panel">
  <div class="panel__head">
    <div>
      <h2>📂 Quản lý Danh mục</h2>
      <p class="muted">Tạo và quản lý các danh mục sản phẩm</p>
    </div>
    <a href="/categories/new" class="btn solid">➕ Thêm danh mục</a>
  </div>

  <% if (categories.length === 0) { %>
    <div style="padding: 64px; text-align: center;">
      <p style="color: var(--muted); font-size: 18px;">Chưa có danh mục nào.</p>
      <a href="/categories/new" class="btn solid" style="margin-top: 16px;">➕ Tạo danh mục đầu tiên</a>
    </div>
  <% } else { %>
    <div class="table-wrap">
      <table class="table">
        <thead>
          <tr>
            <th>Tên danh mục</th>
            <th>Mô tả</th>
            <th>Ngày tạo</th>
            <th>Thao tác</th>
          </tr>
        </thead>
        <tbody>
          <% categories.forEach(category => { %>
            <tr>
              <td><strong><%= category.name %></strong></td>
              <td style="color: var(--muted);"><%= category.description || '—' %></td>
              <td>
                <%= new Date(category.createdAt).toLocaleDateString('vi-VN') %>
              </td>
              <td>
                <div class="row-actions">
                  <a href="/categories/<%= category.categoryId %>/edit" class="link">✏️ Sửa</a>
                  <form action="/categories/<%= category.categoryId %>?_method=DELETE" method="POST">
                    <button 
                      type="submit" 
                      class="link danger"
                      onclick="return confirm('Bạn có chắc muốn xóa danh mục này? (Sản phẩm sẽ giữ nguyên)')"
                    >
                      🗑️ Xóa
                    </button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>

  <div style="margin-top: 24px;">
    <a href="/" class="link">← Quay lại Sản phẩm</a>
  </div>
</div>
